{% macro color_legend(colors) -%}
<div class="color-legend pt-1 pb-1">
    {% for label, color in colors.items() %}
    <span class="legend-unit">
        <div class="color-square" style="background-color: {{ color }};"></div>
        <div class="color-label">{{ label.replace("_", " ").title() }}</div>
    </span>
    {% endfor %}
</div>
{%- endmacro %}

{% macro icon_legend(icons) -%}
<div class="color-legend">
    {% for label, icon in icons.items() %}
    <span class="legend-unit">
        <div class="icon">{{ icon }}</div>
        <div class="icon-label">{{ label.replace("_", " ").title() }}</div>
    </span>
    {% endfor %}
</div>
{%- endmacro %}

{% macro spreadsheet_error_legend() -%}
<div class="color-legend pt-1 pb-1">
    {% for error in SpreadSheetErrors %}
    <span class="legend-unit">
        <div class="color-square" style="background-color: {{ error.color }};"></div>
        <div class="color-label">{{ error.title }}</div>
    </span>
    {% endfor %}
</div>
{%- endmacro %}
{# Index badge colors: the same classes index_badge_class() (server/utils/barcodes.py) gives the badges #}
{% macro index_badge_legend() -%}
<span class="badge badge-success">Kit</span> <span class="badge badge-primary">Validated</span> <span class="badge badge-warning">Not Validated</span> <span class="badge badge-danger">Not Specified</span>
{%- endmacro %}

{% macro index_color_legend() -%}
{{ color_legend({
    "kit": "var(--color-cemm-green)",
    "validated": "var(--color-cemm-blue)",
    "not_validated": "var(--color-cemm-yellow)",
    "not_specified": "var(--color-cemm-red)",
}) }}
{%- endmacro %}
