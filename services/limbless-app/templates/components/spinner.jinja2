{% macro spinner(id=none) -%}
<div class="d-flex justify-content-center htmx-indicator" style="width: 100%; overflow-y: hidden; overflow-x: hidden;" {% if id %}id="{{ id }}"{% endif %}>
    <div class="spinner-border cemm-blue" role="status">
        <span class="visually-hidden">Loading...</span>
    </div>
</div>
{% endmacro %}